function Navbar(){

    return (
        <nav className="navbar bg-primary bg-gradient">
            <div className="container-fluid">
                <span className="navbar-brand mb-0 h1 text-white">WikiCountries</span>
            </div>
        </nav>
    )
};

export default Navbar;